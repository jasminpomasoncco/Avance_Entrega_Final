/*
const { Router } = require('express');
const shoppingCartController = require('../controllers/productController');

const shoppingCart = Router()

shoppingCart.get('/:id/products', shoppingCartController.getCartByIdProduct)
shoppingCart.post('/', shoppingCartController.createCart)
shoppingCart.post('/:id/products', shoppingCartController.createCartByIdProduct)
shoppingCart.delete('/:id/productos/:id_prod', shoppingCartController.deleteProductByIdProduct)
shoppingCart.delete('/:id', shoppingCartController.deleteCart)

module.exports = shoppingCart
*/